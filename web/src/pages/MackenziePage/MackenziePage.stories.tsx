import type { Meta, StoryObj } from '@storybook/react'

import MackenziePage from './MackenziePage'

const meta: Meta<typeof MackenziePage> = {
  component: MackenziePage,
}

export default meta

type Story = StoryObj<typeof MackenziePage>

export const Primary: Story = {}
