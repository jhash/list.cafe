import type { Meta, StoryObj } from '@storybook/react'

import ListDraftPage from './ListDraftPage'

const meta: Meta<typeof ListDraftPage> = {
  component: ListDraftPage,
}

export default meta

type Story = StoryObj<typeof ListDraftPage>

export const Primary: Story = {}
